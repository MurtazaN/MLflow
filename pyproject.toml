[project]
name = "wine-quality-pipeline"
version = "0.1.0"
description = "MLflow Wine Quality Prediction Pipeline"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src*"]
